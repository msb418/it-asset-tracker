# Environment Variables Example for IT Trouble Ticketing App

# MongoDB connection string (replace with your own MongoDB URI)
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/it-trouble-ticketing?retryWrites=true&w=majority

# NextAuth secret (used for encrypting JWTs)
NEXTAUTH_SECRET=your-secret-key

# NextAuth URL (e.g. for development: http://localhost:3000)
NEXTAUTH_URL=http://localhost:3000

# Admin email address (used to seed the admin account)
ADMIN_EMAIL=admin@example.com

# SMTP configuration for email notifications (if used)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your-smtp-user
SMTP_PASS=your-smtp-password

